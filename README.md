# OTUS JavaScript QA Engineer Project

## Installation

To set up the project, run:

```bash
npm install
```

To execute the test suite:

```bash
npm run test
```

To use ci for install dependencies and run tests:
```bash
npm ci
```

To install Playwright dependencies:
```bash
npx playwright install --with-deps
```