export const config = {
    baseUrl: "https://petstore.swagger.io/v2"
};

export const petUrl = config.baseUrl;
export const userUrl = `${config.baseUrl}/user`;
export const tokenUrl = `${config.baseUrl}/user/token`;